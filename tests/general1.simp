/*
    Verify that the general format of a file is parsed correctly
*/
import "some_file";

int sadf;
int asd123;
int sdfsd = 2+2;

struct asdf {
    def weriuy();
    def qwe();
    int blart ;
    float opire;
    list werwer[];
    list werwer;
    dict sad[] = {asdf:123, asd:"asd", wer:{asd:123, asdf:[1, 2, 34]}};
    list zxc[] = [1, 2, 3];
};

def sdf(qwe, qwe, qwe) {
    def qwe() {
        int asd = 12*2/3;
    }
    while(asd.qwe neq 100.01) {
        switch(asd.wer.ew) {
            case (123) {
                if(asd) {
                    asdf();
                }
                else (qwek) { dict asdf; qwer(asddf); }
                else {
                    int asd= 123;
                }
            }
            case (asd) {
                asdas(asd);
            }
            default {}
        }
    }
    // init a type struct
    asdf = {.asd=1+2, .opire={.one=1, .two=2, .three=3}, .sad=0, .wer = sdf};
}

// create list definition vs list reference
// create dict definition vs reference
